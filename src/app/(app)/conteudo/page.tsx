import {PageHeading} from "@/components/page-heading";import {SettingsForm} from "@/components/settings-form";import {OpenAIConfigForm} from "@/components/openai-config-form";import {prisma} from "@/lib/db";
export default async function Conteudo(){const setting=await prisma.appSetting.findUnique({where:{key:"content"}});return <><PageHeading title="Configurações de conteúdo" subtitle="OpenAI, prompt, imagens e conversões de unidades."/><div className="space-y-5"><OpenAIConfigForm/><SettingsForm initial={(setting?.value??{}) as Record<string,unknown>}/></div></>}
