import { prisma } from "@/lib/db"; import { PageHeading } from "@/components/page-heading"; import { SyncButton } from "@/components/sync-button";
export default async function Dashboard(){const [sources,last,exports]=await Promise.all([prisma.dataSource.count({where:{active:true}}),prisma.syncRun.findFirst({orderBy:{createdAt:"desc"}}),prisma.exportRun.count()]);const counts=(last?.counts??{}) as Record<string,number>;const cards:[[string,number],[string,number],[string,number],[string,number]]=[["Fontes ativas",sources],["Pendentes",counts.pending??0],["Com estoque",counts.withStock??0],["Exportações",exports]];return <><PageHeading title="Visão geral" subtitle="Acompanhe a saúde das fontes e as prioridades do cadastro." action={<SyncButton/>}/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label,value])=><div className="card" key={label}><p className="text-sm font-semibold text-gray-500">{label}</p><p className="mt-3 text-4xl font-black">{value}</p></div>)}</div><div className="mt-5 grid gap-5 lg:grid-cols-[1.4fr_1fr]"><div className="card"><h2 className="font-bold">Última atualização</h2>{last?<div className="mt-5 space-y-3"><div className="flex justify-between"><span className="text-gray-500">Status</span><span className="badge badge-orange">{last.status}</span></div><div className="flex justify-between"><span className="text-gray-500">Progresso</span><strong>{last.progress}%</strong></div><div className="h-2 overflow-hidden rounded-full bg-gray-100"><div className="h-full bg-orange-500" style={{width:`${last.progress}%`}}/></div><p className="text-sm text-gray-500">{last.createdAt.toLocaleString("pt-BR")}</p></div>:<p className="mt-4 text-sm text-gray-500">Nenhuma atualização executada.</p>}</div><div className="card"><h2 className="font-bold">Próximos passos</h2><ol className="mt-4 space-y-3 text-sm text-gray-600"><li>1. Configure as cinco fontes.</li><li>2. Envie o modelo do Tiny.</li><li>3. Atualize os dados e revise pendências.</li><li>4. Selecione e exporte os produtos.</li></ol></div></div></>}
