import {NextResponse} from "next/server";import {z} from "zod";import {assertSameOrigin,requireSession} from "@/lib/auth";import {prisma} from "@/lib/db";import {normalizeSku} from "@/lib/normalization";
const schema=z.object({sku:z.string().min(1),weight:z.number().nullable().optional(),length:z.number().nullable().optional(),width:z.number().nullable().optional(),height:z.number().nullable().optional(),additionalInformation:z.string().optional(),approvedDescription:z.string().optional(),imageUrls:z.array(z.string().url()).optional()});
export async function PUT(request:Request){try{assertSameOrigin(request);await requireSession();const body=schema.parse(await request.json());const skuKey=normalizeSku(body.sku);const data={...body,sku:undefined,skuKey};const result=await prisma.productOverride.upsert({where:{skuKey},update:data,create:data});return NextResponse.json(result)}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Dados inválidos."},{status:400})}}
