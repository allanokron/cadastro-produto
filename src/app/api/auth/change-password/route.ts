import {NextResponse} from "next/server";import {compare,hash} from "bcryptjs";import {z} from "zod";import {assertSameOrigin,requireSession} from "@/lib/auth";import {prisma} from "@/lib/db";
const schema=z.object({currentPassword:z.string().min(1),newPassword:z.string().min(10).max(200)});
export async function POST(request:Request){try{assertSameOrigin(request);const session=await requireSession();const body=schema.parse(await request.json());if(!await compare(body.currentPassword,session.user.passwordHash))return NextResponse.json({error:"Senha atual incorreta."},{status:400});await prisma.user.update({where:{id:session.user.id},data:{passwordHash:await hash(body.newPassword,12),mustChangePassword:false}});await prisma.session.deleteMany({where:{userId:session.user.id,id:{not:session.id}}});return NextResponse.json({ok:true})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Falha ao alterar senha."},{status:400})}}
