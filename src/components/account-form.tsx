"use client";import {useState} from "react";import {useRouter} from "next/navigation";
export function AccountForm({mustChange}:{mustChange:boolean}){const [message,setMessage]=useState("");const router=useRouter();async function save(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const d=Object.fromEntries(new FormData(e.currentTarget));if(d.newPassword!==d.confirm)return setMessage("A confirmação não coincide.");const r=await fetch("/api/auth/change-password",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(d)});const result=await r.json();setMessage(r.ok?"Senha alterada com sucesso.":result.error);if(r.ok){e.currentTarget.reset();router.refresh()}}return <form onSubmit={save} className="card max-w-2xl space-y-4">{mustChange&&<div className="rounded-xl border border-orange-200 bg-orange-50 p-4 text-sm font-semibold text-orange-800">Troque a senha inicial antes de continuar a operação.</div>}{message&&<p className="rounded-lg bg-gray-50 p-3 text-sm">{message}</p>}<div><label className="label">Senha atual</label><input className="input" name="currentPassword" type="password" required/></div><div><label className="label">Nova senha (mínimo 10 caracteres)</label><input className="input" name="newPassword" type="password" minLength={10} required/></div><div><label className="label">Confirmar nova senha</label><input className="input" name="confirm" type="password" required/></div><button className="btn btn-primary">Alterar senha</button></form>}
