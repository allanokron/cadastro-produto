"use client";import {useState} from "react";
export function SettingsForm({initial}:{initial:Record<string,unknown>}){const [message,setMessage]=useState("");async function save(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/settings",{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify({...data,imageStart:Number(data.imageStart),imageCount:Number(data.imageCount)})});setMessage(r.ok?"Configurações salvas.":"Não foi possível salvar.")}return <form className="card space-y-5" onSubmit={save}>{message&&<p className="rounded-lg bg-green-50 p-3 text-sm text-green-700">{message}</p>}<div><label className="label">Prompt global de descrição</label><textarea className="input min-h-40" name="aiPrompt" defaultValue={String(initial.aiPrompt??"")}/></div><div><label className="label">Padrão da URL externa</label><input className="input" name="imageUrlPattern" defaultValue={String(initial.imageUrlPattern??"")}/></div><div className="grid gap-4 sm:grid-cols-3"><div><label className="label">Primeiro número</label><input className="input" type="number" name="imageStart" defaultValue={Number(initial.imageStart??1)}/></div><div><label className="label">Quantidade</label><input className="input" type="number" name="imageCount" defaultValue={Number(initial.imageCount??5)}/></div><div><label className="label">Extensão</label><input className="input" name="imageExtension" defaultValue={String(initial.imageExtension??"jpg")}/></div></div><div className="grid gap-4 sm:grid-cols-4">{["sourceWeightUnit","targetWeightUnit","sourceDimensionUnit","targetDimensionUnit"].map(k=><div key={k}><label className="label">{k}</label><input className="input" name={k} defaultValue={String(initial[k]??"")}/></div>)}</div><button className="btn btn-primary">Salvar configurações</button></form>}
